reverse_iterator
enable_if

map stack set

Any function implementation put in a header file (except for function templates) means 0 to the exercise?

checking NULL from allocator?

know what explicit means


remove assertions
do TODOs

up next:
	now rewriting my Avltree to use a pointer to the root node. It's a bit messy, because the nodes methods still assume they can be accessed from anywhere.
